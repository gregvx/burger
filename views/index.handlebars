<h1>Eat-Da-Burger!</h1>

<div class="container">
  <div class="row">
    <div class="uneaten col-sm">
      <h2>Burgers that are not devoured:</h2>
      <ul class="list-group">
        {{#each burgers}} {{#unless devoured}} {{> burger-block devoured=true}} {{/unless}} {{/each}}
      </ul>
    </div>
    <div class="eaten col-sm">
      <h2>Burgers that are devoured:</h2>
      <ul>
        {{#each burgers}} {{#if devoured}} {{> burger-block devoured=false}} {{/if}} {{/each}}
      </ul>
    </div>
  </div>
</div>


<h3>Enter a burger below to create it:</h3>
<form class="create-form">

  <div class="form-group">
    <label for="burg">Burger Name</label>
    <input type="text" id="burg" name="burger_name">
    <button type="submit" class="btn">Create</button>
  </div>

  {{!--
  <div class="form-group">
    <label for="devo">Devoured?</label><br>
    <input type="radio" name="devo" value="1" checked> Devoured<br>
    <input type="radio" name="devo" value="0"> Not Devoured
  </div> --}}

</form>
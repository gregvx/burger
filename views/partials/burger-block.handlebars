<li class="list-group-item list-group-item-warning">
	{{id}}.) {{burger_name}}

	<button class="change-devoured btn btn-light" data-id="{{id}}" data-newstatus="{{devoured}}">
		{{#if devoured}}Eat Me{{else}}Regurgitate{{/if}}
	</button>
</li>
